server:
  port: 9090
  tomcat: 
    threads:
      max: 200

---
spring:
  config:
    activate:
      on-profile: docker
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/

---
spring:
  config:
    activate:
      on-profile: vm
eureka:
  client:
    serviceUrl:
      defaultZone: http://141.22.10.81:8761/eureka/